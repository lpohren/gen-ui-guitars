[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build", "ln -sf .output dist"]

[start]
cmd = "npx vite preview --host 0.0.0.0 --port $PORT"

